<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:background="?android:attr/selectableItemBackground"
    android:layout_marginHorizontal="2dp"
    android:layout_marginVertical="1dp"
    android:padding="4dp">

    <TextView
        android:id="@+id/tvChannelListName"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@id/tvChannelListNumber"
        app:layout_constraintBaseline_toBaselineOf="@id/tvChannelListNumber"
        android:fontFamily="@font/lato_bold"
        android:paddingHorizontal="5dp"
        android:maxLines="1"
        android:ellipsize="end"
        tools:text="Nombre de canal" />

    <TextView
        android:id="@+id/tvChannelListNumber"
        android:textColor="@color/white"
        android:layout_width="25dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/lato_bold"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="20sp"
        app:layout_constraintStart_toStartOf="parent"
        android:gravity="end"
        tools:text = "00"/>
</androidx.constraintlayout.widget.ConstraintLayout>